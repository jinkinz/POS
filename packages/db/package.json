{
  "name": "@pos/db",
  "version": "0.1.0",
  "main": "./index.js",
  "types": "./index.d.ts",
  "scripts": {
    "generate": "prisma generate",
    "validate": "prisma validate",
    "migrate:dev": "prisma migrate dev",
    "build": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.2.0"
  },
  "devDependencies": {
    "prisma": "^6.2.0"
  }
}
